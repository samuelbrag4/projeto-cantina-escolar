<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <title>Dashboard</title>
    <link rel="stylesheet" href="/style.css">
</head>

<body>
    <header>
        <h2>Bem-vindo(a), <%= usuario ? usuario.nome : 'Usuário' %>!</h2>
        <nav>
            <a href="/cadastro-produto">Cadastro de Produto</a> |
            <a href="/gestao-estoque">Gestão de Estoque</a> |
            <a href="/logout">Sair</a>
        </nav>
    </header>

    <main>
        <h3>Estatísticas</h3>
        <p>Total de produtos: <%= totalProdutos %></p>
        <p>Total de vendas: <%= totalMov %></p>

        <h3>Produtos com estoque baixo</h3>
        <% if (!produtosBaixos || produtosBaixos.length === 0) { %>
            <p>Nenhum produto com estoque baixo.</p>
        <% } else { %>
            <table>
                <thead>
                    <tr><th>Produto</th><th>Quantidade</th><th>Preço</th></tr>
                </thead>
                <tbody>
                    <% produtosBaixos.forEach(p => { %>
                        <tr>
                            <td><%= p.nome %></td>
                            <td><%= p.quantidade %></td>
                            <td>R$ <%= Number(p.preco).toFixed(2) %></td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        <% } %>
    </main>
</body>

</html>